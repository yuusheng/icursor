{
  "name": "ios-cursor",
  "version": "0.0.1",
  "description": "Make your cursor in web looks like ios",
  "author": "yuusheng <hi@yuusheng.im>",
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "play": "npm -C playground run dev",
    "lint": "eslint .",
    "build": "rm -rf packages/*/dist && pnpm -r --filter=./packages/* run build"
  },
  "dependencies": {
    "@ios-cursor/core": "workspace:^0.0.1"
  },
  "devDependencies": {
    "@yuusheng/eslint-config": "^0.0.4",
    "eslint": "^8.29.0",
    "typescript": "^4.9.3",
    "vite": "^3.2.4",
    "unbuild": "^1.0.1"
  },
  "eslintConfig": {
    "extends": "@yuusheng",
    "rules": {
      "no-new": "off"
    }
  }
}
